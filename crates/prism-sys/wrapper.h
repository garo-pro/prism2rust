/* SPDX-License-Identifier: MPL-2.0
 *
 * bindgen entry point. Includes the single public header of the pinned
 * upstream Prism submodule. Regeneration is driven by build.rs (with the
 * `bindgen` feature) and the `update-bridge` maintenance skill.
 */
#include "prism.h"
